# Virtual Environment
venv/
env/
.ci_venv/

# Python Cache
__pycache__/
*.py[cod]

# Secrets & Local Configuration
.env

# Local Caches & Temp Databases
chroma_db/
.cache/
fastembed_cache/
.pytest_cache/

# Eval artifacts (regenerated by eval.py)
eval_report.json
eval_gold_report.json

# Raw source PDFs (public SEC filings — re-fetchable via ingest.py --force-download)
data/*.pdf

# Tooling session artifacts
.zcode/
# Secrets backups (NEVER commit)
.env.bak*

# Personal artifacts
*.pdf
unseen_results.json
q2_attempt*.json
coverage_report.json
